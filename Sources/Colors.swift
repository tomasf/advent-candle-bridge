import Cadova

enum DisplayColors {
    static let body = Color.red.adjusting(brightness: -0.3)
    static let candle = Color.white
    static let bottomPanel = Color.white
    static let socketMount = Color.slateGray
}
